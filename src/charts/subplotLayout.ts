/** Left / right panels for dual-axis series + rolling correlation (Charts 2 & 7). */
export const SIDE_BY_SIDE_CORR = {
  leftX: [0.05, 0.58] as [number, number],
  rightX: [0.73, 0.94] as [number, number],
  y: [0.08, 0.92] as [number, number],
  corrRange: [-1, 1] as [number, number],
};

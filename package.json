{
  "name": "economic-monitor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite",
    "fetch-data": "node scripts/fetch-economic-data.mjs",
    "ml:prepare": "cd ml && python3 -m data.prepare_panel",
    "ml:train": "cd ml && python3 -m train.train_all",
    "prebuild": "node scripts/fetch-economic-data.mjs",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/plotly.js": "^2.35.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "onnxruntime-web": "^1.22.0",
    "plotly.js-dist-min": "^2.35.3"
  }
}
